# MeowSense runtime + research dependencies.
# Tested on Python 3.11.
#
# Install with:
#   pip install -r requirements.txt
#
# Note: torch is only needed for src/train_cnn.py. If you only want the
# MFCC baselines (src/train_baseline.py), you can safely skip it.

numpy>=1.24,<3
scipy>=1.10
scikit-learn>=1.3
torch>=2.0
matplotlib>=3.7
pandas>=2.0
jupyter>=1.0
